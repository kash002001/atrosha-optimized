import BatchClient from "./BatchClient";

export const metadata = {
    title: "Batch AP — Atrosha",
    description: "Multi-invoice batch processing and approval workflow.",
};

export default function BatchPage() {
    return <BatchClient />;
}
