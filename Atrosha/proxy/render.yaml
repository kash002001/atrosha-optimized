services:
  - type: web
    name: atrosha-proxy
    runtime: docker
    dockerfilePath: ./dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: RUST_LOG
        value: atrosha_proxy=info
      - key: PORT
        value: "10000"
